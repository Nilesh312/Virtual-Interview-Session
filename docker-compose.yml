version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - SECRET_KEY=${SECRET_KEY:-change_me}
    volumes:
      - ./uploads:/app/uploads
    restart: unless-stopped
